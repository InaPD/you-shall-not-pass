# Copy to .env (gitignored). Never commit real keys.
ANTHROPIC_API_KEY=

# Model IDs (spec 6). Verify against the models overview page before the first paid run.
DOORMAN_AGENT_MODEL=claude-sonnet-5
DOORMAN_READER_MODEL=claude-haiku-4-5
DOORMAN_TEMPERATURE=0
